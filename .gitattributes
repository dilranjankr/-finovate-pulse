# Normalize line endings to LF in the repo (Coolify builds on Linux).
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.svg text eol=lf
*.woff binary
*.woff2 binary
